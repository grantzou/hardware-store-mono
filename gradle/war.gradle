apply plugin: "war"

bootWar {
    mainClass = "com.hardwarestore.simple.HardwareStoreSimpleApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
